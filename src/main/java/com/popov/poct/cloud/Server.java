package com.popov.poct.cloud;

/**
 * Thymeleaf template engine example
 */
public class Server {
    /*public static void main(String args[], Settings settings, Stage primaryStage) {
        port(9090);
        post("/authCallback", (rq, rs) -> {
            if (settings.isDebug()) System.out.println(rq.body());
            //Cloud.authComplete(rq.body(), primaryStage);
            return "cxv";
        });
    }*/
}